{
    "_comment": "CAPAROC 日誌設定檔",

    "log_level": "INFO",

    "retention_days": 0,

    "log_dir": "logs",

    "write_jsonl": true,

    "remote": {
        "_comment": "未來擴充：推送 log 到 Linux 主機。將 enabled 改為 true 並填入 url 即可生效",
        "enabled": false,
        "type": "http",
        "url": "",
        "batch_size": 50,
        "flush_interval_sec": 60,
        "token": ""
    }
}

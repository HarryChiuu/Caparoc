name: caparoc_breaker
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # 核心依賴（必須）
      - pycomm3>=1.2.14
      
      # 開發與測試工具（可選，按需安裝）
      # - pytest>=7.4.0
      # - pytest-cov>=4.1.0
      # - flake8>=6.0.0
      # - black>=23.0.0
      # - ipython>=8.12.0

# Caparoc_breaker_control - Python Dependencies
# CAPAROC 電子斷路器遠端控制程式所需套件
# 實際環境: Python 3.12, conda env sv

# ============================================
# 核心依賴 (必須安裝)
# ============================================
pycomm3>=1.2.14          # EtherNet/IP 和 CIP 通訊協議

# ============================================
# Web UI 依賴 (Phase 4.0)
# ============================================
fastapi>=0.136.0         # Web 框架
uvicorn[standard]>=0.47.0   # ASGI 伺服器（含 websockets）
jinja2>=3.1.6             # HTML 模板引擎
python-multipart>=0.0.28  # Form data 支援
websockets>=14.0          # WebSocket 協議實作（uvicorn 依賴，顯式列出）
requests>=2.32.0          # 設備原廠 Web 介面 HTTP JSON API（/webif/systeminfo, /webif/processdata）

# ============================================
# 開發與測試工具 (可選)
# ============================================
# ruff>=0.16.0            # 靜態檢查（設定見專案根目錄 ruff.toml）
#                         #   python -m ruff check .        檢查
#                         #   python -m ruff check . --fix  自動修正
# pytest>=7.4.0           # 單元測試框架
# pytest-cov>=4.1.0       # 測試覆蓋率
# ipython>=8.12.0         # 增強型互動式 Python shell
#
# 註：不使用 black／formatter——既有程式碼風格一致，全檔重排會淹沒 git blame。
#     flake8 由 ruff 取代（同樣的 pyflakes 規則，速度快兩個數量級且設定集中）。

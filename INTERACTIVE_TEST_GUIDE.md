# 互動式測試指南

## 🚀 啟動程式

```bash
python src/caparoc_controller.py
```

程式會自動執行以下步驟：
1. **全域系統狀態檢查** - 檢測電壓、電流、模組數量
2. **詢問是否初始化通道** - 可選擇跳過，保持設備當前狀態
3. **進入互動模式** - 顯示可用命令並等待輸入

---

## 📋 可用命令

### 🔴 主開關控制（緊急停止用）

```bash
main on      # 開啟主開關（總電源）
main off     # 關閉主開關（總電源）
```

**說明：**
- 控制整個系統的總電源
- 關閉會立即停止所有通道供電
- 用於緊急停止或系統維護

**範例輸出：**
```
> main on

[主開關] 開啟主電源
         byte[0]: 0x80 -> 0x81
         ✅ 已寫入設備
         [狀態] 全域狀態: 0x00
         [狀態] 總電流: 0.50A
```

---

### 🔧 標稱電流設定

**重要：此命令僅顯示手動設定指引！**

```bash
init 2 4     # 顯示 CH2 設定為 4A 的手動操作步驟
```

**範例輸出：**
```
> init 2 4

⚠️  標稱電流無法透過 EtherNet/IP 修改
    請依照以下步驟在設備上手動設定:

【CH2 標稱電流設定步驟】
1. 長按 PWR 鍵 3 秒
   → LED 閃綠光 3 次（解除硬體鎖）

2. 短按 CH2 按鈕進入編程模式
   → 顯示目前設定值

3. 按 + 或 - 調整為 4A

4. 短按 CH2 按鈕確認

5. 長按 PWR 鍵 3 秒退出

設定完成後，使用 'verify 2' 確認
```

**原因：**
- CAPAROC 設備的 Config Assembly 和 Parameter Object 都是**唯讀**的
- 這是設備韌體的安全設計，避免遠端誤操作
- 必須在設備本地手動設定

---

### ✅ 驗證標稱電流

```bash
verify 2     # 驗證 CH2 的標稱電流設定
```

**範例輸出：**
```
> verify 2

[驗證Debug] Input Assembly offset 9:
            Byte 0 (status): 0x00
            Byte 1 (nominal): 4A
            Byte 2: 0x00
✅ CH2 標稱電流: 4A
```

---

### ▶️ 通道控制

```bash
on 1         # 開啟通道 1
off 1        # 關閉通道 1
```

**範例輸出：**
```
> on 2

[控制] CH2 -> 開啟
       byte[1]: 0x80 -> 0x82

[DEBUG] 寫入資料長度: 18 bytes
[DEBUG] byte[0]: 0x80, byte[1]: 0x82
[DEBUG] 寫入 Assembly.0x64
✅ 寫入成功
```

---

### 📊 狀態查詢

```bash
s            # 顯示完整系統狀態
```

**範例輸出：**
```
> s

📊 讀取設備狀態...

🌐 全域系統狀態:
   ✅ 正常

📊 系統參數:
   電壓: 24.00 V
   全域總電流: 8.50 A  (設備報告)
   模組數量: 1 個

📍 通道狀態:

CH1:
  狀態: 🟢 開啟
  標稱電流: 4 A
  實際電流: 2.5 A (63%)
  詳細狀態: ✅

CH2:
  狀態: 🟢 開啟
  標稱電流: 10 A
  實際電流: 3.5 A (35%)
  詳細狀態: ✅

CH3:
  狀態: 🟢 開啟
  標稱電流: 10 A
  實際電流: 6.0 A (60%)
  詳細狀態: ⚠️ 80% 警告

CH4:
  狀態: 🔴 關閉
  標稱電流: 6 A
  實際電流: 0.0 A (0%)
  詳細狀態: ✅
```

---

### 📡 即時監控

#### 啟動監控（靜默模式，推薦）

```bash
monitor start 2 silent     # 每 2 秒更新，僅警報
```

**範例輸出：**
```
> monitor start 2 silent

✅ 即時監控已啟動
   更新頻率: 2.0s
   模式: 靜默模式 (僅警報)
   💡 提示: 監控在背景運行，有變化時會自動通知

> on 2    # 可以正常輸入指令

======================================================================
🔔 監控警報 [14:32:17]
======================================================================
  ▸ CH2 狀態變更: 開啟
  ▸ 電流變化: 0.0A → 3.5A
======================================================================
>         # 自動恢復輸入提示
```

#### 啟動監控（顯示模式）

```bash
monitor start 5 display    # 每 5 秒顯示完整狀態
```

**範例輸出：**
```
> monitor start 5 display

✅ 即時監控已啟動
   更新頻率: 5.0s
   模式: 顯示模式 (持續更新)

======================================================================
🔄 即時監控 [14:32:15] - 更新頻率: 5.0s
======================================================================
📊 系統: 24.0V | 8.5A | 1 模組

通道            狀態   電流         警告/錯誤
----------------------------------------------------------------------
CH1             🟢 開  2.5A / 4.0A  ✅
CH2             🔴 關  0.0A / 10.0A ✅
CH3             🟢 開  6.0A / 10.0A ⚠️80%
CH4             🔴 關  0.0A / 6.0A  ✅
======================================================================
```

#### 停止監控

```bash
monitor stop
```

#### 查看監控狀態

```bash
monitor status
```

---

### 🔍 診斷命令

```bash
scan         # 掃描所有 Assembly Instance
limits       # 顯示通道配置限制
```

---

### 🚪 退出程式

```bash
q            # 退出互動模式
```

---

## 🧪 完整測試流程

### 測試 1：基本通道控制

```bash
# 1. 啟動程式
python src/caparoc_controller.py

# 2. 跳過初始化（保持當前狀態）
是否需要初始化通道? [y/N]: N

# 3. 驗證標稱電流
> verify 1
> verify 2

# 4. 開啟通道
> on 1
> on 2

# 5. 查看狀態
> s

# 6. 關閉通道
> off 1
> off 2

# 7. 退出
> q
```

### 測試 2：主開關控制

```bash
# 1. 啟動程式並跳過初始化
python src/caparoc_controller.py
是否需要初始化通道? [y/N]: N

# 2. 開啟部分通道
> on 1
> on 2

# 3. 查看狀態（確認通道開啟）
> s

# 4. 緊急停止（關閉主開關）
> main off

# 5. 查看狀態（所有通道應停止供電）
> s

# 6. 恢復供電
> main on

# 7. 查看狀態
> s

# 8. 退出
> q
```

### 測試 3：即時監控

```bash
# 1. 啟動程式並跳過初始化
python src/caparoc_controller.py
是否需要初始化通道? [y/N]: N

# 2. 啟動靜默監控
> monitor start 2 silent

# 3. 測試通道開關（觀察警報）
> on 1
# 等待警報顯示

> on 2
# 等待警報顯示

> off 1
# 等待警報顯示

# 4. 查看監控狀態
> monitor status

# 5. 停止監控
> monitor stop

# 6. 退出
> q
```

### 測試 4：標稱電流設定（手動）

```bash
# 1. 啟動程式並跳過初始化
python src/caparoc_controller.py
是否需要初始化通道? [y/N]: N

# 2. 查看設定指引
> init 2 4

# 3. 在設備上手動設定
#    （按照顯示的步驟操作）

# 4. 驗證設定
> verify 2

# 5. 測試通道
> on 2
> s

# 6. 退出
> q
```

---

## ⚠️ 常見問題

### Q1: 為什麼 `init` 命令不會自動設定電流？

**答：** CAPAROC 設備的配置參數為**唯讀**，無法透過 EtherNet/IP 修改。這是設備韌體的安全設計。

### Q2: 如何確認標稱電流設定成功？

**答：** 使用 `verify <ch>` 命令：
```bash
> verify 2
✅ CH2 標稱電流: 4A
```

### Q3: 主開關和通道開關有什麼區別？

**答：**
- **主開關 (`main on/off`)**: 控制總電源，影響所有通道
- **通道開關 (`on/off <ch>`)**: 控制個別通道

### Q4: 靜默監控和顯示監控有什麼區別？

**答：**
- **靜默模式 (`silent`)**: 背景監控，**不干擾命令輸入**，僅有變化時顯示警報（推薦）
- **顯示模式 (`display`)**: 定期顯示完整狀態，會干擾命令輸入

### Q5: 遇到連接問題怎麼辦？

**答：** 運行診斷工具：
```bash
python check_connection.py
```

詳細排查請參閱 [連接問題排查指南](docs/TROUBLESHOOTING_CONNECTION.md)

---

## 📚 相關文檔

- **[README.md](README.md)** - 專案概述
- **[docs/MAIN_POWER_CONTROL.md](docs/MAIN_POWER_CONTROL.md)** - 主開關技術細節
- **[docs/TROUBLESHOOTING_CONNECTION.md](docs/TROUBLESHOOTING_CONNECTION.md)** - 連接診斷

---

**最後更新**: 2025-10-29  
**適用版本**: v3.6+

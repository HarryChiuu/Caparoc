#!/usr/bin/env python3
"""
應用程式版本號 — 全專案唯一真相來源。

── 為什麼需要這支模組 ──────────────────────────────────────────────
`docs/TODO.md` 技術債 #11：前端資源的 cache-busting 版號 `?v=` 原本**手寫在
index.html 兩處**（stylesheet 與 app.js），改版時只要漏改一處，使用者的瀏覽器
就會沿用舊的快取檔案，回報「新功能沒出現」——沒有錯誤訊息、沒有 log，
是 TODO 自評「最難查」的一種故障。而且當時全專案**沒有任何版本常數**，
連個可以對照的來源都沒有。

現在 index.html 改用 `{{ app_version }}` 佔位符，由 `web/app.py` 在回應時
替換（見該檔 `_render_index()`）。要發版就只改這裡一個數字。

── 版號規則 ────────────────────────────────────────────────────────
`__version__` 對應 docs/CHANGELOG.md 的版本，語意為 MAJOR.MINOR.PATCH。
前端資源快取以此字串為鍵：**只要前端檔案有變動就必須進版**，否則舊快取不會失效。

⚠️ 不要在這裡 import 任何專案模組。本模組被 `web/app.py` 與未來的打包腳本
（Phase 5.3）共用，必須保持零相依，才能在任何情境下安全載入。
"""

__version__ = "4.15.0"

# 前端靜態資源的 cache-busting 版號。目前與應用程式版本一致；
# 若日後需要在不進主版號的情況下強制前端更新，可在此獨立調整。
ASSET_VERSION = __version__

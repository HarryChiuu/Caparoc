# 互動式命令測試指南

## 🚀 啟動程式

```bash
python src/caparoc_controller.py
```

選擇選項 **2** (互動控制模式)

---

## 📋 可用命令

### 1️⃣ 設定標稱電流 (新功能!)

```
> init 2 4
```

**預期輸出:**
```
[初始化] CH2: 設定額定電流 4A
       [Param] 使用 Parameter Object 方法
       [Param] EDS Param 9 (M1.CH2) = 4A
       ✅ Parameter 寫入成功
       ✅ 驗證成功: 設備回報 4A
✅ CH2 初始化完成
```

### 2️⃣ 驗證設定

```
> verify 2
```

**預期輸出:**
```
✅ CH2 標稱電流: 4A
```

### 3️⃣ 開啟通道

```
> on 2
```

### 4️⃣ 查看狀態

```
> s
```

### 5️⃣ 啟動監控 (可選)

```
> monitor start 2 silent
```

在監控運行時仍可輸入其他命令！

---

## 🧪 完整測試流程

```bash
# 1. 啟動程式並選擇互動模式
python src/caparoc_controller.py
選擇: 2

# 2. 設定 CH2 = 4A
> init 2 4

# 3. 設定 CH3 = 4A  
> init 3 4

# 4. 驗證設定
> verify 2
> verify 3

# 5. 開啟通道
> on 2
> on 3

# 6. 查看完整狀態
> s

# 7. 啟動監控 (可選)
> monitor start 2 silent

# 8. 在監控運行時仍可設定其他通道
> init 1 10
> on 1

# 9. 退出
> q
```

---

## ✅ 測試重點

### 1. Parameter Object 方法 (1-20A)
- [ ] `init 1 1` - 測試 1A (最小值)
- [ ] `init 2 4` - 測試 4A (常用值)
- [ ] `init 3 10` - 測試 10A (LED 上限)
- [ ] `init 4 20` - 測試 20A (最大值)

### 2. 驗證功能
- [ ] 每次 init 後使用 `verify` 確認
- [ ] 檢查回報值是否正確

### 3. 錯誤處理
- [ ] `init 1 0` - 應拒絕 (< 1A)
- [ ] `init 1 21` - 應拒絕 (> 20A)
- [ ] `init 99 4` - 應拒絕 (通道不存在)

### 4. 整合測試
- [ ] 設定後立即開啟通道
- [ ] 使用監控模式觀察電流
- [ ] 多通道同時操作

---

## 🐛 如果出現問題

### "Parameter 寫入失敗"
可能原因:
1. 參數被鎖定 (programming lock = 1)
2. 設備處於特殊模式
3. 網路連線問題

**解決方案:**
- 程式會自動嘗試 LED 按鈕模擬 (僅支援 1-10A)
- 檢查設備旋轉開關是否在 'RC' 位置

### "驗證失敗"
可能原因:
1. 設備需要時間處理配置
2. 設定值與實際值不同

**解決方案:**
- 重新執行 `init` 命令
- 使用 `verify` 再次確認

### 監控干擾輸入
**解決方案:**
- 使用靜默模式: `monitor start 2 silent`
- 或停止監控: `monitor stop`

---

## 📝 測試記錄

| 命令 | 預期結果 | 實際結果 | 通過? |
|------|---------|---------|-------|
| `init 2 4` | 設定成功 | | [ ] |
| `verify 2` | 回報 4A | | [ ] |
| `init 3 20` | 設定成功 | | [ ] |
| `verify 3` | 回報 20A | | [ ] |
| `on 2` | 開啟成功 | | [ ] |
| `s` | 顯示狀態 | | [ ] |

---

## 💡 提示

1. **建議流程**: init → verify → on → monitor
2. **監控模式**: 使用 silent 模式可在背景運行不干擾輸入
3. **範圍限制**: 
   - Parameter Object: 1-20A ✅
   - LED 按鈕模擬: 1-10A (備用)
4. **即時性**: 無需重啟程式即可設定多個通道
